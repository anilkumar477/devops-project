apiVersion: batch/v1
kind: Job
metadata:
  name: nel-job
spec:
  template:
    spec:
      containers:
      - name: hello
        image: busybox:1.36
        command:
        - sh
        - -c
        - echo "Hello from Nel Kubernetes Job!"
      restartPolicy: Never
