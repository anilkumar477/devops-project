apiVersion: batch/v1
kind: Job
metadata:
  name: nel-failed-job
spec:
  backoffLimit: 3
  template:
    spec:
      containers:
      - name: failure
        image: busybox:1.36
        command:
        - sh
        - -c
        - echo "Job starting..."; exit 1
      restartPolicy: Never
